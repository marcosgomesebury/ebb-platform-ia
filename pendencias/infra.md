# 🏗️ Pendências de Infraestrutura

Itens relacionados a infra, firewall e deploy de serviços.

- [ ] **ebb-pix-spi**: Pendente em Dev, Staging e Prod (Namespace `rails`). Código em `ebb-iac-resource/ebb-money-flows`.
- [ ] **ebb-conciliacao**: Pendente regras de firewall (Namespace `finance`). Código em `ebb-iac-resource/ebb-money-flows`.
- [ ] **Pub/Sub (Infra)**: Criação de Tópicos e Subscriptions em todos os projetos `ebb-money-flows` (`ebb-iac-resource`).
- [ ] **Ingress (HSM)**: Validar e ajustar configurações de ingresso.
- [ ] **Migração IAC**: `ebb-money-flows` ainda reside em `ebb-iac-resource` (singular), enquanto `ebb-ebury-connect` já está no novo `ebb-iac-resources` (plural).
